apiVersion: v1
kind: Service
metadata:
  name: {{ include "threatintelrelay-chart.fullname" . }}
  labels:
    {{- include "threatintelrelay-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "threatintelrelay-chart.selectorLabels" . | nindent 4 }}
