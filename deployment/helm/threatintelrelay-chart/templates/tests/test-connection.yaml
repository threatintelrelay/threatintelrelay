apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "threatintelrelay-chart.fullname" . }}-test-connection"
  labels:
    {{- include "threatintelrelay-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "threatintelrelay-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
